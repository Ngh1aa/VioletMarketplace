# Design Intelligence Decision Log — Violet V4

## Status

`N/A_JUSTIFIED` for Phase 1 retrieval execution.

Project Config sets `design_intelligence: AUTO`. The locked Website Delivery Pipeline requires retrieval only when it can materially improve an unresolved active decision; installed corpus is not automatically active.

## Assessment

After project-truth audit + production reference research, Phase 1 has sufficient evidence to resolve:
- multi-house IA;
- discovery/sample journey;
- product-first hierarchy;
- page-role composition diversity;
- media roles;
- Violet signature and anti-patterns.

There is no remaining design-system decision that requires vendor-corpus retrieval to avoid a generic or undefined contract. Activating it now would add context without changing an unresolved decision.

## Rule for later phases

Design Intelligence may be activated in Phase 2/3 only for a **targeted implementation or rendered finding** that project truth + local skills + reference evidence do not resolve. If activated:
- use the locked repository ref;
- one narrow `--domain` or verified stack query;
- retry once maximum;
- record ADOPT/ADAPT/REJECT;
- never let retrieval silently override the Phase-1 Design Contract;
- rendered evidence, not retrieval output, determines PASS.